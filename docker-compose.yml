# docker-compose.yml - Updated for Docker Hub
services:
  nextjs:
    # Use your Docker Hub image instead of local build
    image: yash12j/nextjs-portfolio-forest:latest
    # Remove the build section if using Docker Hub image
    # build: .
    ports:
      - "3001:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
